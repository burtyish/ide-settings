import classNames from "classnames";
import React from 'react';
import styles from "./style.scss";

interface I${COMPONENT_NAME}Props {
}

export class ${COMPONENT_NAME} extends React.Component<I${COMPONENT_NAME}Props, {}> {
    public render() {
        return (
        
        );
    }
}